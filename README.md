Utility bash and batch scripts to emulate git push/pull with internal files and folders.
Copy-paste the scripts into your program directory, and run them. They should just work.

TODO: Fix and unhardcode regex for .internal_gitignore.txt in push/pull bash scripts.
TODO: Write batch scripts